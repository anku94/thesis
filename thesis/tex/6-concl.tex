% !TEX root = ../main.tex
\chapter{Conclusions and Future Directions}\label{chap:concl}
\begin{center}
  \begin{minipage}{0.95\textwidth}
    \minitoc
  \end{minipage}
\end{center}




This thesis argues that a large class of efficiency bottlenecks in BSP systems arise from
conditions unknowable before runtime, and that such conditions are systematically addressable
through adaptive mechanisms if the requisite feedback loops exist.
% \todo{Example margin note - default style} \todo[inline]{Example inline TODO - spans full width,
% good for longer notes} \todo[color=green! 40]{Green margin note - use color for categories}
% \minitoc% TODO% 1.% Unifying overlay topologies (CARP + intra-tier shuffling) 2.% Open table
% formats as a natural fit for BSP data 3.% Convergence between algebras 4.% ML-specific differences
% 5.% DAQs as a parallel?% 6.% DeltaFS vs ORCA: filesystems vs databases/analytics.% Open Table
% Formats are at the heart.% 7.% View-driven Observability% 6.0 chapter intro: thesis claim,
% evidence recap, OLAP × BSP discussion, chapter roadmap

\begin{itemize}
  \item In CARP (\cref{chap:carp-paper}), the loop is policy-driven: a static renegotiation policy
        periodically reconstructs a
        global view of attribute distributions and uses it to drive adaptive range partitioning
        as distributions evolve.

  \item In the AMR study (\cref{chap:amr-paper}), the loop was human-driven: placement effects were
        repeatedly confounded
        by fail-slow hardware, fabric behavior, and tuning issues, and progress depended on
        us manually constructing views from fine-grained BSP telemetry to isolate and localize
        these confounders.
        The resulting slow, rerun-driven diagnostic process illustrates the difficulty of
        addressing similar anomalies in production environments.

  \item ORCA (\cref{chap:orca-paper}) enables a programmable feedback loop via a SQL-based on-fabric
        analytics pipeline that
        materializes BSP telemetry views through in-situ reduction, coupled with
        timestep-consistent control semantics for coordinated runtime intervention.
        The loop driver is programmable and can range from human operators to automated agents.
\end{itemize}

Beyond observability, ORCA's design reflects a broader convergence between OLAP and HPC:
columnar formats~\cite{:ApacheArrow, :ApacheParquet}, SQL planning, and embeddable query
engines~\cite{Lamb2024:DataFusion} applied to in-situ HPC analytics.
Compared with prior in-situ systems, ORCA differs in two ways.
First, instead of placing analytics entirely on application
ranks~\cite{Zheng2018:DeltaFS,Jain2024:CARP} or entirely on a dedicated
tier~\cite{Eisen2025:ADIOS}, ORCA's planner decomposes them into operators that can be placed on
either tier at runtime.
Second, unlike ADIOS-style offload as a peer MPI job~\cite{Eisen2025:ADIOS}, ORCA's analytics tier
is fully asynchronous while still preserving timestep-consistent semantics.

We conclude this thesis by recapping the steerable observability paradigm and its limits
(\S\ref{sec:concl:retrospective}), synthesizing broader lessons for scientific data management
from CARP and ORCA (\S\ref{sec:concl:olap}), describing extensions within immediate reach
(\S\ref{sec:concl:extensions}), and outlining longer-term directions (\S\ref{sec:concl:future}).

% ORCA realizes a programmable tree-based overlay for in-situ analytics using OLAP primitives such
% as columnar formats, SQL planning, and embeddable query engines. Compared with prior in-situ HPC
% systems, ORCA differs in two ways. First, it enables a hybrid execution model. Instead of
% monolithic analytics placed entirely on application ranks~\cite{Zheng2018:DeltaFS,Jain2024:CARP}
% or entirely offloaded to a dedicated tier~\cite{Eisen2025:ADIOS}, ORCA uses a planner to decompose
% analytics into operators that can be placed on either tier at runtime. Second, unlike ADIOS-style
% offload as a peer MPI job~\cite{Eisen2025:ADIOS}, ORCA’s analytics tier is completely asynchronous
% while still preserving timestep-consistent analytics semantics.

% The rest of this chapter reflects on this design point (\S\ref{sec:concl:retrospective}),
% describes extensions within immediate reach (\S\ref{sec:concl:extensions}), and outlines
% longer-term directions (\S\ref{sec:concl:future}).

\section{Steerable Observability: Retrospective and Synthesis}\label{sec:concl:retrospective}


Steerable observability is the ability to request runtime views of application state, reconstructed
in real time, and to change those views on demand as diagnosis progresses.
% We define steerable observability as the ability to request runtime views of application state,
% reconstructed in real time, with the ability to change requested views on demand. This makes
% always-on collection viable for production BSP systems: high-level signals flag anomalies in real
% time, and narrower views can then surface their root causes. The same paradigm enables online
% intervention, as views are both necessary to guide interventions and assess their effectiveness.
% Its core contribution is economic.
Conventional tracing is locked to a preselected fidelity that both overcollects and undercollects:
despite paying for comprehensive collection at one layer, it can still lack the
visibility into lower layers necessary for root-cause identification.
In contrast, steerable observability materializes relevant views directly from the application in
real-time.
This drastically reduces collection and storage overhead through predicate and reduction pushdowns.
It also shortens feedback latency, enabling drill-downs across arbitrary fidelity insofar as
instrumentation permits, while cheap pushdowns enable continuous predicate evaluation for
anomalies.
% In contrast, steerable observability materializes relevant views directly from the application in
% real-time. Not only does this drastically reduce the collection and storage overhead by enabling
% predicate and reduction pushdowns, but it also shortens the feedback latency, enabling drilldowns
% across arbitrary fidelity insofar as instruementation permits. Steerable observability shifts the
% cost model by evaluating predicates and reductions before export, so only matches and summaries
% pay serialization, transport, and storage. This makes needle-in-haystack questions viable as
% continuous queries, and supports drill-down across arbitrary fidelities as needed, insofar as
% instrumentation permits.



% \looseness=-1
\paragraph{BSP Is Different}
BSP workloads run as a single coordinated job, not a web of loosely coupled microservices.
Global synchronization defines both the unit of work and the axes of causality and consistency.
Attribution of pathologies is first to an anomalous rank, and then to local events
within that rank.
Root causes lie within a deeper layer of a single workload's runtime stack, not across
microservices,
requiring analytical substrates to preserve these causal edges.
Event-based streaming systems~\cite{Kreps2011:Kafka,Carbo2015:ApacheFlink} treat
events as separate in transit, requiring these dependency edges to be reconstructed after
collection.
\hl{Even with identical collection profiles and data formats,
  reconstructing these edges after persistence incurs costs from small random reads,
  analogous to how FastQuery compares with CARP in} \S\ref{sec:carp:eval_carpdb}.

% Not only does ORCA's in-situ timestep alignment produce a more efficient on-disk layout, but its
% co-design and in-situ analytics translate queries into an execution strategy that approaches the
% minimum necessary work.
\looseness=-1
\hl{ORCA's in-situ timestep alignment not only accelerates offline trace analytics but enables
  skipping
  persistence entirely---only the relevant data is materialized for a given view, and queries
  translate into execution strategies that approach the minimum necessary work.
}
Data movement is minimized via pushdowns that leverage workload resources, and RDMA minimizes
the residual data movement overhead.
Prior~\cite{Erlin2011:Fay,Berg2021:Snicket,Mace2015:Pivottracing} work has explored query-driven
tracing for asynchronous microservices.
ORCA extends this to BSP workloads, adding a control plane for steerability.
% ORCA is co-designed for this paradigm, translating queries into an execution strategy that
% approaches the minimum necessary work --- data movement is minimized via pushdowns that leverage
% workload resources, and RDMA minimizes the residual data movement overhead.
% Fay~~\cite{Erlin2011:Fay}, Snicket~\cite{Berg2021:Snicket}, and Pivot
% Tracing~\cite{Mace2015:Pivottracing} implement query-driven tracing for microservices. straggler
% rank, then to the sequence of events behind its slowdown. Analyses need to preserve these causal
% dependencies. All processing happens in-memory over a highly efficient columnar representation.
% The closest analogs are \emph{Data Acquisition} (DAQ) pipelines in particle
% accelerators~\cite{Aboli2016:DAQ-ATLAS,Bauer2012:DAQ-CMS}, where FPGAs discard the bulk of sensor
% data at source, retaining only high-signal telemetry.



\paragraph{Steerability Limits}
Feedback-driven mechanisms have an inherent latency floor: collection, reduction, and transport
take physical time.
ORCA eliminates the artificial costs that dominate today---ETL and unnecessary data
movement---but cannot circumvent this floor.
Some regimes demand action faster than any global view can be assembled.
Network congestion control algorithms, for instance, are designed explicitly around partial
observability~\cite{Jacob1988:CCAPartial}.
When conditions are too transient for online intervention, ORCA's in-situ reductions still capture
their structure more efficiently than any post-hoc approach, providing the empirical basis for
developing predictive strategies that anticipate rather than react~\cite{Smith1957:Predict}.

\paragraph{Applicability Beyond BSP}
\hl{
While this thesis focuses on steerability under BSP constraints,
the broader lesson is that
observability becomes far more powerful when built over an explicit ontology of system state rather
than over fixed telemetry categories such as traces, metrics, and logs.
BSP makes this unusually tractable because it defines a regular unit of distributed
progress with strong consistency guarantees that applies regardless of scale.
For observability beyond BSP, ORCA suggests two directions.
Conceptually, it argues for
richer ontologies, modeling systems as not just serving requests asynchronously, but as
databases, filesystems, and inference engines, extending what can become visible and
actionable, with instructive parallels in} Palantir~\cite{:PalantirOntology}.
\hl{Architecturally, it is a template for observability as tier-aware streaming
analytics evaluating invariants over distributed application state, and persisting detail
only when
those invariants are violated---closer to a distributed core dump than to always-on retention.
Extending this model beyond BSP, however, requires solving the harder problem that BSP largely
sidesteps: defining the ontology, causal boundaries,
and staleness bounds in systems that do not provide a global clock for free,
and deriving from them meaningful pushed-down units of work.
}


\section{Lessons from OLAP for Scientific Data Management}\label{sec:concl:olap}

Having reviewed ORCA as an observability and control substrate, we now turn to broader lessons from
CARP and ORCA for scientific data management.

\paragraph{Why HPC Ingestion Is Filesystem-Centric}
HPC applications write to parallel
filesystems~\cite{Braam2002:Lustre,Schmu2002:GPFS,:VDURA,2025:VASTAI,:WekaIO,:Hammerspace} and
object
stores~\cite{Liang2020:DAOS} designed to
absorb synchronized bursts of data from tens of thousands of ranks, instead of databases.
% % Why do HPC and OLAP get along so well?% What does it imply for the future of scientific data?%
% What is ingestion?% What is ingestion?
Workflows may additionally interpose intermediate staging tiers~\cite{Liu2012:RoleBurstBuffers}.
While data management techniques such as partitioning are deeply relevant,
they must be realized as in-situ streaming pipelines that reorganize data while minimizing
ingestion overhead, terminating in files and objects.
The finite nature of HPC jobs, along with the heterogenous and application-specific nature of
scientific data formats, resists monolithic databases.


% HPC applications write to parallel filesystems. Lustre, DAOS, WekaFS, and similar systems are
% co-designed to absorb synchronized write bursts from thousands of ranks. Staging workflows may
% interpose burst buffers, but the endpoint is always a filesystem. Every application generates
% different data types under different schemas, and workloads are transient. A monolithic database
% that owns the entire data lifecycle is the wrong shape for this environment. If you want streaming
% analytics for BSP, the primitive is in-situ reduction materializing results into this existing
% storage stack, not ingestion into a centralized store.

\paragraph{Relevance of Open Table Formats}
Modern analytics systems increasingly decouple query engines from storage.
Open table formats such as Iceberg~\cite{:Iceberg} and Delta Lake~\cite{Armbr2020:Deltalake}
maintain a
lightweight catalog over partitioned
columnar files or objects.
Query engines attach at read time and use this catalog for pruning, pushdown, and lazy evaluation.
CARP and ORCA converge on the same pattern.
CARP range-partitions particle data into contiguous key and value blocks with metadata as a
separate manifest, structurally identical to Parquet and Iceberg.
ORCA writes trace data as timestep-partitioned Parquet but does not currently generate a catalog.
Open table formats are immediately applicable to other forms of scientific data, insofar as they
fit the
relational model.

% Modern analytics trends mirror this decoupling of query engines from storage. Open table formats
% like Iceberg and Delta Lake maintain a lightweight catalog over partitioned columnar files or
% objects. Query engines attach at read time and leverage the catalog for pushdown, pruning, and
% lazy evaluation. CARP and ORCA independently converge on this pattern. CARP range-partitions
% particle data into a custom format comprising contiguous key and value blocks, with metadata as a
% separate manifest --- structurally identical to Parquet and Iceberg. ORCA writes trace data as
% timestep-partitioned Parquet, but does not currently generate a catalog. This pattern is
% immediately applicable to other forms of scientific data, insofar as it fits the relational model.

\paragraph{Challenges for Scientific Catalog Formats}
Scientific data formats extend beyond the relational model.
While particle data and telemetry streams map cleanly to tables, other simulation paradigms write
multidimensional arrays and custom metadata via formats like HDF5~\cite{Folk2011:HDF5},
netCDF~\cite{:NetCDF},
Zarr~\cite{:Zarr}, and XDMF~\cite{Clark2007:XDMFE}.
Among these, Zarr is closest to a catalog-backed layout, having evolved for GIS data stored in
cloud
object stores.
Its metadata enables index-based pruning,
but it lacks value-level statistics for filter predicates.
arrow-zarr~\cite{2026:arrow-zarr} is an early attempt to bridge Zarr and Arrow~\cite{:ApacheArrow}.
Despite these gaps, simulation structure already aligns with the broader pattern: problem
decomposition
intrinsically partitions state in many codes, and the main task is to preserve that partitioning
through I/O and
capture it in a catalog.
On the analytics side, such catalogs would enable query engine-based access to scientific data,
extending the benefits of pushdown, pruning, and planning beyond relational workloads.
DataFusion's~\cite{Lamb2024:DataFusion} extensibility makes this a viable path.

% Not all scientific data is tabular. Particle data and telemetry streams map cleanly to the
% relational model, but other simulation paradigms write multidimensional arrays and custom metadata
% via formats like HDF5, netCDF, Zarr, XDMF etc. Zarr is the closet to a catalog format, as it
% volved for GIS data stored in cloud. It stores statistics for enabling index-based pushdowns, but
% not value-based statistics like Iceberg to enable filter predicates. arrow-zarr is an early
% attempt to bridge Zarr and Arrow. But the structure of simulations aligns: problems are already
% partitioned because of decomposition, we just need to preserve it in the I/O and capture a
% snapshot in the catalog. On the analytics front, catalogs can enable interacting with this data
% via query engine-based approaches can extending the benefits of pushdowns, pruning, and planning
% to scientific data. DataFusion makes extension viable.

% meshes, AMR hierarchies, and irregular tensors need richer logical schemas. A scientific catalog
% must encode array semantics: shapes, coordinates, topology, neighborhood relations, and indexing
% structures that enable domain-aware pruning across dimensions like spatial extent, block ID, AMR
% level, or timestep. The append-mostly nature of simulation output simplifies consistency, but
% atomic commits across many ranks and timesteps, manifest scaling under synchronized parallel
% writes, and small-file pressure on parallel filesystem metadata services are real engineering
% challenges. Zarr and arrow-zarr represent early movement in this direction from the GIS community,
% offering an array-native format closer to Iceberg than HDF5 in its design assumptions.

% \paragraph{ORCA as Scientific Data Infrastructure} ORCA's tree-based overlay is naturally
% positioned to synthesize catalogs during collection. As data flows through the TBON, reductions
% can generate partition statistics and catalog metadata alongside the data itself. The relational
% algebra currently implemented via DataFusion handles tabular telemetry and particle data. But
% DataFusion is extensible. Building a library of scientific kernels and providers for array and
% mesh data would bring the same lazy-query-with-pushdown ecosystem to a broader class of scientific
% datasets. Combined with CARP-style intra-tier shuffles composed as operators in the same query
% plan and scheduled phase-aware to avoid interference, this points toward a unified analytics
% substrate for scientific data: a system where you point at data on a parallel filesystem and write
% query plans, and the system handles placement, pushdown, and execution.

% Is ingestion writing to ClickHouse? No.

% HPC applications write to parallel filesystems. Lustre, DAOS, pNFS, WekaFS, Hammerspace. Burst
% buffers and staging workflows may be included. These are designed for the paradigm. Parallel
% filesystems are co-designed and sized to handle synchronized bursts. Workflows may need complex
% staging: write to burst buffers then drain to filesystems. Every application generates different
% datatypes. As a result it is not practical to have a database.

% Ingestion has traditionally been via different scientific formats like HDF5 or XDMF. Coupled with
% MPI-IO. People are increasingly finding out that Parquet is suitable for at least certain types of
% data. Particle data tracks fixed entities with properties: something that fits the relational
% model. Meshes and unstructured stuff is more complicated.

% We keep stumbling upon similar findings. CARP generated a Parquet-like format + a catalog. If you
% squint, it looks like Iceberg. ORCA generates partitioned Parquet. Iceberg is a natural stepping
% stone, although currently not implemented. As object store interfaces become more common, open
% table formats like this are a natural step forward. Maybe HDF5 can be expressed as Parquet +
% metadata. Maybe not.

% How does this compare to a database as a monolithic entity? IMO this is a decoupling of the ingest
% and the query sides. DeltaFS and CARP implement in-situ transforms but ultimately generate a bunch
% of per-rank logs + an object file. Insofar as data fits Iceberg, this is excellent. Query-side
% concerns are pretty simple as data is not mutated once written. You only need query plans to
% accelerate reads on arrays and all. Xarray is relevant here.

% The question then arises: what is the end goal of scientific data? IMO decoupled catalogs are
% still a natural fit. Each rank already emits a partitioned version of the entire dataset, because
% problem decomposition creates partitioning. You just need to capture the metadata into a catalog.
% HDF5 can plausibly be represented as Parquet + metadata. After all it's just contiguous arrays.
% That metadata doesn't necessary need to be Iceberg.

% The opportunity this opens up is more universal query engines that can pushdown scans and all for
% different analytics.

% ORCA is naturally in the middle of this. The TBON enables catalogs to be synthesized along with
% catalogs. Currently it implements a relational algebra with DataFusion. Works well out of the box
% for relational data. But this toolkit is very extensible. Can build a library of kernels and
% providers for a scientific catalog. As we do that, complex workflows get easier and easier. ORCA
% combined with CARP can be a unified Kafka + Flink + Spark for scientific data.

\section{Immediate Extensions}\label{sec:concl:extensions}

\paragraph{Multi-Dimensional Parallelism for ML Training}
Large-scale ML training implements multiple levels of parallelism --- data, tensor, pipeline, and
expert --- that compose into complex multi-dimensional
topologies~\cite{Gratt2024:Llama3Scale,Rahma2024:MLScale}.
While the execution model remains fundamentally BSP at each level, the nesting creates structure
that ORCA does not currently exploit.
The TBON topology and the query planner would benefit from alignment to these parallelism
dimensions, enabling intra-data-parallel reductions to be pushed deeper in the tree than
cross-data-parallel aggregations.
The ML training ecosystem currently lacks a standard way to construct and query these topologies
at runtime, so this would require integration with training frameworks such as
Megatron~\cite{Shoey2020:Megatron-LM} and DeepSpeed~\cite{:DeepSpeed}
to discover the parallelism layout and map it onto ORCA's overlay.

\paragraph{Shuffling-Based Streaming Analytics}
Shuffling-based in-situ indexing systems like DeltaFS~\cite{Zheng2018:DeltaFS} and
CARP~\cite{Jain2024:CARP} show that hash and range
partitioning can reorganize scientific data effectively at scale.
ORCA can treat these shuffles as intra-tier operators within its topology, so they compose cleanly
with inter-tier reductions in a single plan.
Because the MPI tier has substantial egress bandwidth, the planner can be
phase-aware, scheduling sinks at the MPI tier in checkpoint phases while avoiding that
interference during compute phases.

\paragraph{Instrumentation Sources}
Tools like Kineto~\cite{2025:PytorchKineto} aggregate telemetry from multiple sources into a
unified trace.
ORCA supports the same but as real-time aggregations over any interface that emits structured
events---CUPTI~\cite{:CUPTI} GPU hooks, MPI runtime probes, hardware counters.
eBPF~\cite{:eBPF} and DynInst~\cite{Berna2011:DynInst} are especially appealing as they enable dynamic instrumentation without
recompilation, extending visibility into arbitrary functions at runtime.

\paragraph{Approximate Summaries}
Mergeable sketches can summarize streams in-flight at far lower cost and fit naturally into ORCA’s
reduction topology.
For example, KLL~\cite{Karni2016:KLLSketch} can estimate latency percentiles, and
Count-Min~\cite{Cormo2005:CountMin} can
identify heavy hitters such as straggler ranks or hottest callsites, all with bounded error and
without retaining raw events.


% \paragraph{Open Table Formats for HPC} While ORCA writes telemetry as partitioned Parquet, it does
% not currently emit a metadata catalog. Open table formats such as Iceberg~\cite{:Iceberg} maintain
% a lightweight index over partitioned Parquet files---partition boundaries, column statistics, file
% locations---enabling engines to prune irrelevant data without scanning. Any HPC pipeline that
% already writes Parquet would benefit from the same treatment, replacing ad hoc file-management
% conventions with self-describing, immediately queryable datasets.

\paragraph{Control Interface Regularization}
The set of controllable actions is currently defined ad hoc per integration.
Regularizing this into typed providers that advertise capabilities, accept structured commands, and
return typed acknowledgments makes control capabilities discoverable and composable,
analogous to a southbound API in SDN~\cite{Braun2014:SDN}.



\section{Future Directions}\label{sec:concl:future}

ORCA's current query model is relational: SQL over event streams, planned and placed across the
overlay.
The power of an algebraic approach is that complex pipelines reduce to compositions of well-defined
operators, each independently placeable and optimizable.
The directions below extend this principle to richer data types, deeper execution tiers, and
adaptive placement.

\paragraph{Beyond Relational Algebra}
Scientific computing and ML operate primarily on arrays,
meshes, and tensors using domain-specific analytics that go beyond the relational model.
\hl{N-D arrays are strided access patterns over contiguous buffers, requiring only dimensional
  metadata
  on top of the columnar representation.
  More broadly, memory is linear regardless of how data is logically shaped, and columnar formats
  capture that linearity directly.
}
Everything above the buffer is metadata.
Prior work on SciQL~\cite{Kerst2011:SciQL} and MeshSQL~\cite{Lee2004:MeshSQL} has explored
declarative interfaces for these types, and practical
convergence is underway through bridges like arrow-zarr~\cite{2026:arrow-zarr} and
Arrow$\leftrightarrow$DLPack~\cite{:DLPackProtocolApache}.
DataFusion's extensibility makes it possible to host array and mesh operators
alongside relational ones in the same query plan.
This also opens a path to expressing HPC visualization
workflows~\cite{Ayach2015:Catalyst,Larse2022:Ascent} as dataflows rather than separate
pipelines.
A related direction is observability over GPU-resident state such as model weights, where
device-side reductions would
emit compact summaries without transferring full tensors to the host.

\paragraph{Multi-Tier Compilation}
ORCA's overlay currently executes all operators on host-side aggregators.
Additional execution tiers are becoming viable: SmartNICs can run Arrow-native reductions on the
fabric itself~\cite{Ulmer2023:ExtendingComposableDatab,Liu2022:ProcessingParticleData}, and eBPF
can push filter and sketch logic into kernel-side probes.
Each tier admits a restricted operator subset under different resource and state constraints.
More broadly, unified intermediate representations and layered compilation are emerging as the path
toward composing data processing across heterogeneous execution
targets~\cite{Jungm2025:DesigningFutureProofData}.
The compilation problem for ORCA --- decomposing a high-level query across
host, NIC, and kernel tiers --- is an instance of this.
% --- deciding which operators run where and how intermediate results flow between them.

\paragraph{Cost-Aware Reactive Placement}
ORCA’s placement today follows the tree topology: per-partition work is pushed down, while
cross-partition work remains at higher tiers.
While this works, it is not cost-aware because it does not react to observed placement conditions
such as backlogs and kernel costs.
A natural extension is to feed these cost signals back into the planner and let it move operators
and sinks across tiers online.
This effectively makes ORCA self-tuning using the same observability machinery it provides to
applications.

% \section{Galaxy Brain Stuff}

% \subsection{Maxwell's Demon}

% It’s relevant as an analogy about “information → usable work via feedback,” not as a literal
% technical framework you should import.

% How it maps cleanly: • Maxwell’s Demon: by measuring microstate and acting on it, the demon seems
% to extract work from thermal fluctuations. The resolution is that information has a cost
% (measurement, memory, erasure; Landauer), so you can’t get “free work. ” • ORCA/CARP: by measuring
% distributed execution/application state and acting on it (placement, throttling, repartition,
% etc.), you extract performance/efficiency from variability/nonstationarity. The “demon tax” is
% observability overhead + coordination cost + perturbation, which has to be paid somewhere.

% So the useful “science” point is:

% Feedback can turn uncertainty/variance into advantage only insofar as the cost of
% acquiring/processing information is less than the benefit of better decisions.

% That’s exactly the tradeoff ORCA formalizes as “query-driven / pay proportional to the question,”
% and what CARP informally exploits with its renegotiation epochs.

% Where the analogy breaks / becomes unhelpful: • Thermodynamics is about entropy/energy with a
% tight formalism; your setting is distributed control with resource tradeoffs (bandwidth, latency,
% CPU, coordination). You can steal the intuition (no free lunch; info isn’t free), but don’t
% pretend there’s a conserved quantity you can compute.

% If you use it at all, it should be a 1–2 sentence motivating metaphor: “ORCA is an information
% engine; measurement + control can harvest slack, but only if measurement costs are bounded. ”

% \subsection{Ashby's Law of Requisite Variety}

% % remove all unicode characters Ashby's Law of Requisite Variety (cybernetics/control) says,
% roughly: a regulator can only suppress disturbances to the extent that the regulator has
% comparable "variety" (i.e., a sufficient repertoire of distinguishable responses / states).
% Ashby's aphorism is commonly rendered as 'only variety can destroy variety'.

% More formally (one common statement): the variety/uncertainty left in the outcomes can't be
% reduced below what remains after accounting for the regulator's variety; in information-theoretic
% form you'll see inequalities like $V_O \ge V_D - V_R$ or $H(E) \ge H(D) - H(R)$ under standard
% assumptions.

% Is it relevant to ORCA/CARP?

% Yes, as a useful lens—but you have to map “variety” correctly. • In Ashby, "variety" is about the
% regulator's ability to select among alternatives. That selection capacity can come from:
% \begin{itemize} \item Action variety (many possible interventions), \item Information/measurement
% that lets you choose the right intervention conditional on the \item Ashby explicitly links
% regulation limits to communication/channel capacity. \end{itemize} and/or Information/measurement
% that lets you choose the right intervention conditional on the disturbance (selection requires
% discrimination).

% CARP: the disturbance is (time-varying) key-distribution skew; the regulator's variety is
% essentially "many possible repartitionings + the mechanism to choose among them," based on
% observed distributions. It's a specialized regulator with a narrow disturbance class but a strong,
% domain-specific action set.

% ORCA: the disturbance class is broader (performance pathologies, imbalance modes, phase changes,
% cross-stack variability). ORCA's claim-to-power is that it expands the regulator's effective
% variety by: • making the measurement space programmable and composable (you can form the
% discriminants you need), and • aligning sensing/actuation to a semantic clock (logical time) so
% the "selection" is coherent at BSP boundaries (i.e., you're choosing based on a well-defined slice
% of the world, not an arbitrary asynchronous smear).

% Under Ashby's lens: ORCA is an attempt to raise $V_R$ (effective regulatory capacity) not just by
% adding knobs, but by increasing the ability to pick the right knob setting in-context—i.e.,
% improving discrimination/selection under uncertainty. That's conceptually aligned with Ashby even
% though you're not importing thermodynamic-style conservation laws.

% (W.Ross Ashby is the original source; his Introduction to Cybernetics is the canonical reference.)
